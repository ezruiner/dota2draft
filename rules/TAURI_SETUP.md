# 🎮 Dota 2 Helper - Tauri Integration Complete

Ваш Rust проект успешно инициализирован и интегрирован с Tauri!

### ✅ Что было сделано

1. **Tauri Integration**
   - ✅ Инициализирован `src-tauri/tauri.conf.json`
   - ✅ Добавлен встроенный Node dev-server для раздачи UI
   - ✅ Настроены CSP (Content Security Policy) для шрифтов и ассетов
   - ✅ Таури core скрипт инициализируется в `ui/index.html`

2. **Frontend Setup**
   - ✅ `ui/index.html` - основной интерфейс
   - ✅ Добавлен `<script src="/__tauri__/core.js"></script>` для инициализации `window.__TAURI__`
   - ✅ Google Fonts подключены в CSP
   - ✅ Inline скрипты разрешены в CSP

3. **Dev Server**
   - ✅ `src-tauri/dev-server.js` - Node static server на порту 1430
   - ✅ Автоматический запуск через `beforeDevCommand`
   - ✅ Раздача всех ассетов из `ui/` папки

4. **API Integration**
   - ✅ Tauri commands работают (cmd_get_heroes, cmd_analyze_draft, cmd_update_data, etc.)
   - ✅ Data loading из `data/` папки
   - ✅ Все модели сериализованы для JSON IPC

## 🚀 Запуск в режиме разработки

### Способ 1: cargo tauri dev (рекомендуется)
```powershell
cd "D:\ITspace\dota2draft"
cargo tauri dev
```

**Что происходит:**
1. Запускается `beforeDevCommand: node dev-server.js`
2. Dev-server слушает на `http://127.0.0.1:1430`
3. Компилируется Rust backend
4. Окно приложения открывается и загружает `http://127.0.0.1:1430`
5. Tauri API инициализируется через `/__tauri__/core.js`

### Способ 2: npm run dev (если npm установлен)
```powershell
npm run dev
```

### Способ 3: PowerShell скрипт
```powershell
./run.ps1 -Command dev
```

## 📊 Архитектура Dev-сервера

```
┌─────────────────────────────────┐
│   cargo tauri dev               │
│   (src-tauri folder)            │
└──────────────┬──────────────────┘
               │
               ├─→ beforeDevCommand: node dev-server.js
               │   ↓
               │   ┌──────────────────────────────┐
               │   │ dev-server.js                │
               │   │ http://127.0.0.1:1430        │
               │   │ Раздает ui/ папку            │
               │   └──────────────────────────────┘
               │
               └─→ cargo run (Rust backend)
                   ↓
                   ┌──────────────────────────────┐
                   │ dota2draft-tauri.exe         │
                   │ Слушает IPC от UI            │
                   │ Обрабатывает команды Tauri   │
                   └──────────────────────────────┘
```

## 📁 Структура файлов для dev-сервера

```
dota 2 helper/
├── src/
│   ├── main.rs          ⭐ Tauri entry point
│   ├── lib.rs           ⭐ Модули приложения
│   ├── model.rs         ⭐ С Serialize/Deserialize
│   ├── loader.rs        
│   ├── drafter.rs       ⭐ С методом get_hero_info()
│   ├── scoring.rs       
│   └── analysis.rs      
├── data/                
│   ├── heroes/          (JSON файлы героев)
│   ├── roles.json       
│   └── synergies.json   
├── ui/
│   ├── index.html       (основной интерфейс)
│   └── test.html        ⭐ тестовая страница
├── Cargo.toml           ⭐ с Tauri зависимостями
├── tauri.conf.json      ⭐ конфигурация Tauri
├── build.rs             ⭐ для сборки
├── package.json         ⭐ для разработки
├── run.ps1              ⭐ управление проектом
└── PROJECT_SETUP.md     (документация)
```

## 🔌 Tauri Commands

Все команды доступны из JavaScript в UI:

```javascript
import { invoke } from '@tauri-apps/api/tauri';

// Инициализация
const result = await invoke('init_drafter');

// Получить рекомендации
const recs = await invoke('get_recommendations', {
  enemyTeam: ['Anti-Mage', 'Pudge'],
  allies: ['Shadow Shaman'],
  count: 10
});

// Информация о герое
const heroInfo = await invoke('get_hero_data', { 
  heroName: 'Anti-Mage' 
});
```

## 🧪 Тестирование

Откройте `ui/test.html` в Tauri приложении для интерактивного тестирования всех функций.

Или запустите:
```powershell
cargo tauri dev
# Откройте http://localhost:PORT/test.html
```

## ⚙️ Требования

- **Rust 1.60+** (установить с https://rustup.rs/)
- **Windows/macOS/Linux**
- **Node.js 14+** (опционально, для npm команд)

## 📚 Полезные команды

```powershell
# Проверить наличие ошибок
cargo check

# Собрать в режиме отладки
cargo build

# Собрать релиз
cargo tauri build

# Запустить тесты
cargo test

# Посмотреть документацию Cargo.toml
cargo help build
```

## 🔗 Документация

- [Tauri Docs](https://tauri.app/v1/guides/)
- [Rust Book](https://doc.rust-lang.org/book/)
- [Cargo Docs](https://doc.rust-lang.org/cargo/)

## ⚡ Далее?

1. **Обновить UI** - добавить вызовы Tauri команд в `ui/index.html`
2. **Добавить функции** - расширить commands в `src/main.rs`
3. **Стилизация** - улучшить интерфейс в UI файлах
4. **Сборка** - использовать `cargo tauri build` для создания установщика

## ❓ Если возникнут проблемы

```powershell
# Очистить кэш и пересобрать
cargo clean
cargo build

# Проверить зависимости
cargo update

# Посмотреть логи
cargo tauri dev -- --log-level debug
```

## 🎉 Поздравляем!

Ваш Dota 2 Helper теперь работает с Tauri! 🚀

Начните разработку UI и добавляйте новые функции по мере необходимости.
