# 🚀 DOTA 2 HELPER - БЫСТРЫЙ СТАРТ

Ваш Rust проект успешно инициализирован с Tauri!

## ⚡ Самый быстрый способ начать

```powershell
# 1. Откройте PowerShell в папке проекта
cd "D:\ITspace\dota 2 helper"

# 2. Запустите разработку
./run.ps1 -Command dev

# Или напрямую
cargo tauri dev
```

Приложение откроется в окне. Откройте DevTools (F12) для отладки.

## 🧪 Тестирование API

После запуска приложения откройте:
```
http://localhost:XXXX/test.html
```

Там вы найдете интерактивное тестирование всех 3 команд:
- ✅ init_drafter
- ✅ get_recommendations  
- ✅ get_hero_data

## 📚 Документация

| Файл | Описание |
|------|---------|
| `TAURI_SETUP.md` | Полный гайд по Tauri интеграции |
| `PROJECT_SETUP.md` | Архитектура и структура проекта |
| `SETUP_CHECKLIST.md` | Чек-лист всех выполненных задач |
| `INTEGRATION_EXAMPLES.js` | Примеры код для интеграции в UI |

## 🎯 Что было сделано

✅ Инициализирован Rust проект  
✅ Интегрирована Tauri v1.5  
✅ Созданы 3 API команды для взаимодействия с UI  
✅ Все модели сериализованы для JSON  
✅ Создана страница тестирования (test.html)  
✅ Написана документация  
✅ Готовы примеры интеграции  

## 📦 Структура

```
src/
  main.rs          ← Tauri entry point с 3 командами
  lib.rs           ← Модули
  model.rs         ← Структуры с Serialize
  loader.rs        ← Загрузка JSON
  drafter.rs       ← Логика драфтинга
  scoring.rs       ← Оценка героев
  analysis.rs      ← Анализ

data/
  heroes/          ← JSON каждого героя
  roles.json       ← Правила ролей
  synergies.json   ← Синергии

ui/
  index.html       ← Основной интерфейс
  test.html        ← Тестирование API
```

## 🔌 API Команды

### 1️⃣ Инициализация
```javascript
await invoke('init_drafter')
```

### 2️⃣ Рекомендации
```javascript
await invoke('get_recommendations', {
  enemyTeam: ['Anti-Mage', 'Pudge', 'Axe'],
  allies: ['Shadow Shaman'],
  count: 10
})
```

### 3️⃣ Информация о герое
```javascript
await invoke('get_hero_data', {
  heroName: 'Anti-Mage'
})
```

## 💻 Команды разработки

```powershell
# Развитие (горячая перезагрузка)
./run.ps1 -Command dev
cargo tauri dev

# Сборка релиза
./run.ps1 -Command build
cargo tauri build

# Проверка ошибок
./run.ps1 -Command check
cargo check

# Тесты
./run.ps1 -Command test
cargo test
```

## 🎨 Интеграция в основной UI

1. Откройте `ui/index.html`
2. Добавьте импорт (уже есть):
   ```html
   <script src="/__tauri__/core.js"></script>
   ```
3. Используйте примеры из `INTEGRATION_EXAMPLES.js`
4. Вызывайте команды через `invoke()`:
   ```javascript
   const { invoke } = window.__TAURI__;
   const result = await invoke('init_drafter');
   ```

## 🐛 Если что-то не работает

```powershell
# Очистить и пересобрать
cargo clean
cargo build

# Проверить синтаксис
cargo check

# Посмотреть ошибки компиляции
cargo build 2>&1
```

## 📥 Требования

- Rust 1.60+ (https://rustup.rs/)
- Windows/macOS/Linux

## ✨ Что дальше?

1. **Протестируйте** - запустите `cargo tauri dev` и откройте test.html
2. **Интегрируйте** - добавьте вызовы invoke() в ui/index.html
3. **Развивайте** - добавляйте новые функции в src/main.rs
4. **Собирайте** - используйте `cargo tauri build` для релиза

## 🎉 Готово!

Приложение полностью готово к разработке!

**Запустите прямо сейчас:**
```powershell
cargo tauri dev
```

---

Есть вопросы? Смотрите подробную документацию в:
- `TAURI_SETUP.md` - полный гайд
- `INTEGRATION_EXAMPLES.js` - примеры кода
- `PROJECT_SETUP.md` - архитектура
